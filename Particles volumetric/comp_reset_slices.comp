#version 460

layout(local_size_x = 1) in;

layout(std430, set = 0, binding = 0) buffer writeonly buff_inside_counter {
    uint vertexCount;
    uint instanceCount;
    uint firstVertex;
    uint firstInstance; 
};

void main() {

    vertexCount = 4u;
    instanceCount = 0u;
    firstVertex = 0u;
    firstInstance = 0u;
}